description: >
This command uses the Slim.AI's hardening process to enhance the security of the provided container image.
By hardening, we're reducing the image's attack surface by removing unnecessary components and tightening configurations.

steps:
  - run:
      name: HARDEN IMAGE
      command: <<include(scripts/cli_harden.sh)>>
